{% extends 'base.html.twig' %}

{% block title %}{{ 'initiative.new.title'|trans }} · {{ 'app.name'|trans }}{% endblock %}

{% block body %}
    <div class="page">
        <div class="page__header">
            <div>
                <div class="breadcrumb"><a href="{{ path('app_initiative_index') }}">{{ 'nav.initiatives'|trans }}</a></div>
                <h1 class="page__title">{{ 'initiative.new.title'|trans }}</h1>
            </div>
        </div>
        {{ include('initiative/_form.html.twig', {button_label: 'action.create'}) }}
    </div>
{% endblock %}
