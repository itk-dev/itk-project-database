<div class="stat-card">
    <div class="stat-card__label">{{ 'dashboard.total'|trans }}</div>
    <div class="stat-card__value" data-live-text="stat-total">{{ total }}</div>
</div>
<div class="stat-card stat-card--green">
    <div class="stat-card__label">{{ 'dashboard.published'|trans }}</div>
    <div class="stat-card__value" data-live-text="stat-published">{{ published }}</div>
</div>
<div class="stat-card stat-card--lime">
    <div class="stat-card__label">{{ 'dashboard.drafts'|trans }}</div>
    <div class="stat-card__value" data-live-text="stat-drafts">{{ drafts }}</div>
</div>
<div class="stat-card stat-card--accent">
    <div class="stat-card__label">{{ 'nav.contacts'|trans }}</div>
    <div class="stat-card__value" data-live-text="stat-contacts">{{ contactCount }}</div>
</div>
