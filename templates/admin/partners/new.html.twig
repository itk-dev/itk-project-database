{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'partner.new.title'|trans }} · {{ 'app.name'|trans }}{% endblock %}

{% block admin_content %}
    <twig:Page:Header :title="'partner.new.title'|trans" style="margin-bottom: var(--itk-space-5);">
        <twig:block name="breadcrumb">
            <div class="breadcrumb"><a href="{{ path('admin_partners') }}">{{ 'nav.partners'|trans }}</a></div>
        </twig:block>
    </twig:Page:Header>
    {{ include('admin/partners/_form.html.twig', {button_label: 'action.create'}) }}
{% endblock %}
