parameters:
    level: 6
    paths:
        - src
        - tests
    excludePaths:
        - src/Kernel.php
        - tests/bootstrap.php
    doctrine:
        # Required (non-nullable) columns are modelled with nullable properties
        # defaulting to null — the conventional Symfony pattern.
        allowNullablePropertyForRequiredField: true
